pub fn run (){
let face = '\u{1F600}';
println!("hay arrays, tupples, booleans, float, char e int {}", face);
}

// en Rust hay Arrays, Tuples, Booleans, Float Integers(desde 8 a 128 bits)
//
//y Characters(char)